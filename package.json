{
  "name": "@alumis/spa",
  "version": "2.0.6",
  "description": "A small, single page application framework.",
  "scripts": {
    "bump": "git add -A & git commit -am 'bumped' && npm version patch && npm publish && git push --follow-tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alumis/spa.git"
  },
  "author": "Emil Müller",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/alumis/spa/issues"
  },
  "devDependencies": {
    "@alumis/observables": "^2.0.11",
    "@alumis/utils": "^2.0.0"
  },
  "peerDependencies": {
    "@alumis/observables": "^2.0.11",
    "@alumis/utils": "^2.0.0"
  }
}
